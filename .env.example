# Example environment variables for the Pastebin app
# Set the API base URL used by the frontend. Keep the /v1/pastebin path if your backend expects it.
VITE_API_URL=http://localhost:3000/v1/pastebin
